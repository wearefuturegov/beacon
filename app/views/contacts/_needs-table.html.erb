      <% if @open_needs.any? %>
        <table class="needs-table">
          <thead>
            <tr>
              <th>Type</th>
              <th>Priority</th>
              <th>Assigned</th>
              <th>Created</th>
            <tr>
          </thead>
          <tbody>
            <% @open_needs.each do |n| %>
              <tr onclick="window.location='<%= need_path(n) %>'" class="<%= n.css_class %>">
                <td><%= link_to n.category.humanize, n %></td>
                <td>
                  <% if n.is_urgent %>
                    <span class="urgent-tag">Urgent</span>
                  <% else %>
                    —
                  <% end %>
                </td>
                <td><%= n.assigned %></td>
                <td><%= n.created_at&.strftime("%-d %B %Y") %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        <p class="no-results">No open needs recorded for this person</p>
      <% end %>