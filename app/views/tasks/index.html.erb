<p id="notice"><%= notice %></p>

<div class="tabs">
  <%= link_to "Just mine", my_tasks_path, controller: :my_tasks, class: "tabs__tab" %>
  <div class="tabs__tab tabs__tab--current">All</div>
</div>

<div class="page-header">
  <h1 class="page-title">All Tasks (<%= @tasks.length %>) </h1>
</div>

<%= render 'shared/tasks_table', tasks: @tasks %>
<%= javascript_pack_tag 'datatables' %>
