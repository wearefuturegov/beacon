document.getElementById("need-fields-container").innerHTML = "<%= j(render 'shared/need-fields-edit' ) %>";
document.getElementById("edit-need-btn").style.display="none"

inputDefaultDate = document.getElementById("show-need-start-on-flatpicker_format")
if(inputDefaultDate) {
  flatpickr('#need_start_on', {
      dateFormat: "d/m/Y",
      allowInput: true,
      defaultDate: [inputDefaultDate.value]
  });
}
